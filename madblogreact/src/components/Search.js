import React from 'react';

const Search = (props)=>{
    return (
        <input type="text" className="form-control" placeholder="Search for posts..." aria-label="Search" aria-describedby="basic-addon2"/>
    );
}

export default Search;